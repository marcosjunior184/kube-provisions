apiVersion: v1
kind: Service
metadata:
  name: caddy-internal
  namespace: proxy
spec:
  selector:
    app: caddy
  ports:
    - name: http
      port: 80        # Only port 80
      targetPort: 80  # Only port 80
      protocol: TCP
  type: ClusterIP     # Internal only
